import styled from "styled-components";

export const Container=styled.div`
  .inputBox{
    display: flex;
    flex-direction: column;
    gap: .5rem;
  }
  span{
    display: none;
  }
  .no-ok{
    display: flex;
    color: red;
  }
`

